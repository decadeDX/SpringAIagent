<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.decadedx.springaiagent.mapper.ActionExecutionMapper">
    <select id="selectByActionIdForUpdate" resultType="io.github.decadedx.springaiagent.entity.ActionExecution">
        SELECT action_id, user_id, session_id, action_type, execution_status, payload, business_id,
               result_summary, failure_code, created_at, updated_at
        FROM action_execution
        WHERE action_id = #{actionId}
        FOR UPDATE
    </select>
</mapper>
